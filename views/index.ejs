<!DOCTYPE html>
<html>
<%- include('../partials/header.ejs') %>
<body>
  <div>
    <h1>This is home page</h1>
  </div>
  <%- include('../partials/nav.ejs') %>
  <div>
    <h1>All blogs</h1>
    <% if(blog.length>0) { %>
      <% blog.forEach(blog=>{ %>
        <h1>
          <%= blog.title %>
        </h1>
        <p>
          <%= blog.description %>
        </p>
        <% }) %>
          <% } %>
  </div>
</body>

</html>